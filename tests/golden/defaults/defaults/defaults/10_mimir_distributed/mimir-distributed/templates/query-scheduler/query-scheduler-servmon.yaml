apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app.kubernetes.io/component: query-scheduler
    app.kubernetes.io/instance: defaults
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: mimir
    app.kubernetes.io/version: 3.0.4
    helm.sh/chart: mimir-distributed-6.0.6
  name: defaults-mimir-query-scheduler
  namespace: defaults
spec:
  endpoints:
    - port: http-metrics
      relabelings:
        - action: replace
          replacement: defaults/query-scheduler
          sourceLabels:
            - job
          targetLabel: job
        - action: replace
          replacement: defaults
          targetLabel: cluster
      scheme: http
  namespaceSelector:
    matchNames:
      - defaults
  selector:
    matchExpressions:
      - key: prometheus.io/service-monitor
        operator: NotIn
        values:
          - 'false'
    matchLabels:
      app.kubernetes.io/component: query-scheduler
      app.kubernetes.io/instance: defaults
      app.kubernetes.io/name: mimir
